---
phase: 04-frontend-persistence-export
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - index.html
autonomous: true
requirements:
  - FRNT-02
  - FRNT-03
  - FRNT-04
  - FRNT-06
  - FRNT-07
  - FRNT-08
  - FRNT-09

must_haves:
  truths:
    - "User can type a subject, select skill level, and submit the form"
    - "Pipeline steps animate in real time with labeled dots and progress bar"
    - "Course renders with title, overview, prerequisites, watch time, modules, and video cards"
    - "Recent searches appear as clickable pills below the form"
    - "Questions accordion is collapsed by default with textareas inside"
    - "Reveal thinking points button renders as a no-op stub"
  artifacts:
    - path: "index.html"
      provides: "SSE pipeline wiring, course rendering, recent searches"
      contains: "new EventSource"
  key_links:
    - from: "index.html"
      to: "/api/course-stream"
      via: "EventSource with subject and skill_level query params"
      pattern: "api/course-stream"
    - from: "index.html"
      to: "course JSON"
      via: "renderCourse function reading modules, videos, questions"
      pattern: "renderCourse"
---

<objective>
Wire the JavaScript in index.html: form submission, SSE pipeline consumption with real-time step animation, course rendering (modules, video cards, questions, connecting questions), recent searches localStorage, and error handling. This is the core interactive behavior of the app.

Purpose: Make the app functional end-to-end — from search to course display.
Output: index.html with complete JS for form, pipeline, course render, and recent searches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-persistence-export/04-CONTEXT.md
@.planning/phases/04-frontend-persistence-export/04-RESEARCH.md
@.planning/phases/04-frontend-persistence-export/04-UI-SPEC.md
@.planning/phases/03-transcript-course-assembly/03-CONTEXT.md
@.planning/phases/04-frontend-persistence-export/04-01-SUMMARY.md
@index.html
@sse.js
@server.js
@tests/unit/frontend.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire form submission, SSE pipeline, and section state management</name>
  <files>index.html</files>
  <read_first>
    - index.html (current state after Plan 01 — read the full HTML structure and placeholder script)
    - sse.js (SSE event names: query_generated, videos_fetched, scored, transcripts_fetched, course_assembled — and payload shapes)
    - server.js (VALID_LEVELS set, query param names: subject, skill_level, error response shapes)
    - .planning/phases/04-frontend-persistence-export/04-RESEARCH.md (Pattern 1: section visibility, Pattern 2: SSE consumption, Pattern 7: pipeline step dot state machine, Pitfall 2: EventSource auto-reconnect, Pitfall 3: TOO_FEW_VIDEOS)
    - .planning/phases/04-frontend-persistence-export/04-UI-SPEC.md (copywriting contract for error messages, pipeline step labels)
    - tests/unit/frontend.test.js (copy scoreBadgeColor, formatDuration, slugify, relativeTime, updateRecentSearches implementations)
  </read_first>
  <action>
Replace the placeholder `<script>` block in index.html with the full JavaScript implementation. Wrap everything in `document.addEventListener('DOMContentLoaded', () => { ... })`. Add `'use strict';` at the top inside the script.

**Pure functions (copy from tests/unit/frontend.test.js):**
- `slugify(title)` — lowercase, replace `[^a-z0-9]+` with `-`, strip leading/trailing hyphens
- `formatDuration(seconds)` — `H:MM:SS` or `M:SS`
- `scoreBadgeColor(score)` — `>=80: '#22c55e'`, `>=60: '#3b82f6'`, `>=40: '#f59e0b'`, else `'#a0a0a0'`
- `relativeTime(isoString)` — days/hours/minutes ago or 'just now'
- `updateRecentSearches(searches, newSubject)` — deduplicate, prepend, cap at 5

**Section state management:**

```javascript
const CONTENT_SECTIONS = ['pipeline-section', 'error-section', 'course-section', 'empty-state'];
function showContentSection(id) {
  for (const sectionId of CONTENT_SECTIONS) {
    document.getElementById(sectionId).hidden = (sectionId !== id);
  }
}
```

**STEP_LABELS map:**
```javascript
const STEP_LABELS = {
  query_generated: 'Generating search queries',
  videos_fetched: 'Searching YouTube',
  scored: 'Scoring videos',
  transcripts_fetched: 'Fetching transcripts',
  course_assembled: 'Building your course'
};
const STEP_ORDER = ['query_generated', 'videos_fetched', 'scored', 'transcripts_fetched', 'course_assembled'];
```

**Pipeline step rendering:**
Create a function `initPipelineSteps()` that builds 5 `.pipeline-step` divs inside `#pipeline-steps`, each with a `.dot` span and a `.step-label` span. All dots start with `var(--color-text-muted)` (pending state).

Create `updatePipelineStep(eventName)`:
- Mark the corresponding step's dot as `.dot-complete` (remove `.dot-active`)
- If there is a next step, mark its dot as `.dot-active`
- Update progress bar: `progressFill.style.width = ((stepIndex + 1) / 5 * 100) + '%'`

**Form submit handler** on `#btn-generate` click:
1. Read `subject` from `#subject-input` (trim whitespace). If empty, return early.
2. Read `skillLevel` from `#skill-level` value.
3. Disable form: set `#btn-generate` text to `'Generating...'`, disable button + input + select.
4. Clear any previous course: `document.getElementById('course-modules').innerHTML = ''`, `document.getElementById('course-header').innerHTML = ''` (per D-04).
5. Show pipeline section: `showContentSection('pipeline-section')`. Call `initPipelineSteps()`.
6. Update recent searches: call `updateRecentSearches`, save to localStorage key `'ylc_searches'`, re-render pills.
7. Open EventSource: `const url = '/api/course-stream?subject=' + encodeURIComponent(subject) + '&skill_level=' + encodeURIComponent(skillLevel);`
8. For each event in STEP_ORDER, add listener:
   - Parse `JSON.parse(e.data)`
   - Call `updatePipelineStep(eventName)`
   - If `eventName === 'course_assembled'`:
     - `es.close()`
     - If `data.error === 'TOO_FEW_VIDEOS'`: call `showError(data.message)` (uses copywriting: "Not enough high-quality videos found. Try a broader search term.")
     - Else: call `renderCourse(data.course, subject, skillLevel)`, then `saveCourseToHistory(data.course, subject, skillLevel)` (saveCourseToHistory is a no-op stub in this plan — implemented in Plan 03)
     - Re-enable form
9. Named `error` event handler:
   - `es.close()`
   - Parse data: `let data; try { data = JSON.parse(e.data); } catch { data = null; }`
   - Call `showError(resolveErrorMessage(data))`, re-enable form
10. `es.onerror` (native EventSource network error):
    - `if (es) es.close()`
    - `showError('Something went wrong generating your course. Check the server logs.')`
    - Re-enable form

**`showError(message)` function:**
- `showContentSection('error-section')`
- Set `#error-message` textContent to the message string

**`resolveErrorMessage(data)` function:**
- If `data && data.code === 'QUOTA_EXCEEDED'`: return `'YouTube API quota exceeded. Try again after midnight Pacific time.'`
- Else: return `'Something went wrong generating your course. Check the server logs.'`

**`enableForm()` / `disableForm()` helpers:**
- Toggle disabled state on `#btn-generate`, `#subject-input`, `#skill-level`
- Reset button text to `'Generate course'` on enable

**Recent searches rendering:**
- `loadRecentSearches()` — read `'ylc_searches'` from localStorage, parse as JSON array, default to `[]`
- `renderRecentSearches(searches)` — clear `#recent-pills`, create `.pill` buttons for each search. Each pill click sets `#subject-input` value to the search term. Show/hide `#recent-searches` section based on array length > 0.
- Call `loadRecentSearches()` and `renderRecentSearches()` on DOMContentLoaded.

**`renderCourse(course, subject, skillLevel)` function:**
This is the main rendering function. It builds the course view from the course JSON contract (Phase 3 CONTEXT.md).

`showContentSection('course-section')`

Build course header inside `#course-header`:
- Title row: `<div class="course-title-row">` containing:
  - `<h2 class="course-title">` — set via textContent to `course.title`
  - `<button class="btn-export" id="btn-export">Export course</button>` — created via createElement, textContent set
- Overview: `<p class="course-overview">` — textContent = `course.overview`
- Prerequisites row: `<div class="prereq-row">` with `<span class="prereq-label">Prerequisites:</span>` then a `.prereq-badge` span per item (textContent for each)
- Watch time: `<p class="watch-time">` — textContent = `'Estimated watch time: ' + course.totalWatchTime`

Build modules inside `#course-modules`:
For each `module` in `course.modules` (index `mi`):
- Create `<details class="module-card">` — add `open` attribute if `mi === 0`
- Create `<summary>`: `<span class="module-title">` with textContent `'Module ' + (mi+1) + ': ' + module.title`, `<span class="module-count">` with textContent `module.videos.length + ' videos'`
- Create `<div class="module-body">`
- Module description: `<p class="module-description">` — textContent = `module.description`
- For each `video` in `module.videos`:
  - Create `<div class="video-card">`
  - Thumbnail: `<div class="video-thumb"><a href="` + video.url + `" target="_blank" rel="noopener"><img></a></div>` — set img.src = `video.thumbnail`, img.alt = `video.title + ' — YouTube thumbnail'`, img.width = 120, img.height = 68
  - Content: `<div class="video-content">`
    - Title: `<p class="video-title">` — textContent = `video.title`
    - Meta: `<p class="video-meta">` — textContent = `video.channelTitle + ' · ' + formatDuration(video.durationSeconds)`
    - Badge row: `<div class="badge-row">`
      - Score badge: `<span class="score-badge">` — textContent = `video.score`, style.background = `scoreBadgeColor(video.score)`
      - If `video.outdated`: `<span class="outdated-badge">` — textContent = `'May be outdated'`
    - Blurb: `<p class="video-blurb">` — textContent = `video.blurb`
    - Questions accordion: `<details class="questions-accordion"><summary>Comprehension questions (3)</summary>` containing question items
    - For each question in `video.questions`:
      - `<div class="question-item">`
      - Label: `<p class="question-label">` — textContent = `(i+1) + '. (' + capitalize(q.type) + ') ' + q.text`
      - Textarea: `<textarea class="question-textarea" placeholder="Write your answer here..."></textarea>`
      - Hint placeholder: `<p class="hint-text" data-video-id="` + video.videoId + `" data-question-index="` + i + `" hidden></p>` (for Phase 5)
    - Reveal hints button: `<button class="btn-hints" data-video-id="` + video.videoId + `">Reveal thinking points</button>` — click handler is a no-op stub: `btn.addEventListener('click', () => { /* Phase 5: POST /api/hint */ })`
    - Watched checkbox: `<div class="watched-row"><input type="checkbox" id="watched-` + video.videoId + `"><label for="watched-` + video.videoId + `">Mark as watched</label></div>` — checkbox change handler is a stub in this plan (Plan 03 wires localStorage)
  - Connecting question at bottom of module body:
    - `<div class="connecting-question">`
    - `<p class="connecting-question-label">Module question</p>`
    - `<p class="connecting-question-text">` — textContent = `module.connectingQuestion`

**Export button handler** (wired inside renderCourse after creating the button):
Attach click handler to `#btn-export` that calls `exportCourse(course, subject, skillLevel)`.

**`exportCourse(course, subject, skillLevel)` function:**
Copy `buildMarkdown` from frontend.test.js. Build markdown string, create Blob, trigger download via anchor click pattern:
```javascript
function exportCourse(course, subject, skillLevel) {
  const md = buildMarkdown(course, subject, skillLevel);
  const blob = new Blob([md], { type: 'text/markdown' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = slugify(course.title) + '.md';
  document.body.appendChild(a);
  a.click();
  document.body.removeChild(a);
  URL.revokeObjectURL(url);
}
```

**Stub functions (replaced in Plan 03):**
- `saveCourseToHistory(course, subject, skillLevel)` — empty function body
- Watched checkbox change handler — no-op

**CRITICAL XSS prevention (T-04-01):**
ALL dynamic content from course JSON or localStorage MUST use `textContent` or `setAttribute`. NEVER assign to `innerHTML` with untrusted data. Only use `innerHTML` for static structural markup (like the pipeline step template where no user data is interpolated).

IMPORTANT: Each DOM element with dynamic text from course JSON (title, overview, blurb, channelTitle, question text, connecting question, prerequisites) MUST be created via `document.createElement` and have its text set via `.textContent`. No string interpolation into innerHTML for any course data.
  </action>
  <verify>
    <automated>cd /Users/camdenhosn/PycharmProjects/Youtube-Education-App && node --test tests/unit/frontend.test.js && grep -c "new EventSource\|renderCourse\|showContentSection\|STEP_LABELS\|updatePipelineStep\|showError\|exportCourse\|buildMarkdown\|updateRecentSearches" index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `new EventSource`
    - index.html contains `function renderCourse(`
    - index.html contains `function showContentSection(`
    - index.html contains `function initPipelineSteps(`
    - index.html contains `function updatePipelineStep(`
    - index.html contains `function showError(`
    - index.html contains `function resolveErrorMessage(`
    - index.html contains `function enableForm(`
    - index.html contains `function exportCourse(`
    - index.html contains `function buildMarkdown(`
    - index.html contains `function slugify(`
    - index.html contains `function formatDuration(`
    - index.html contains `function scoreBadgeColor(`
    - index.html contains `function updateRecentSearches(`
    - index.html contains `function loadRecentSearches(`
    - index.html contains `function renderRecentSearches(`
    - index.html contains `'ylc_searches'`
    - index.html contains `api/course-stream`
    - index.html contains `skill_level`
    - index.html contains `encodeURIComponent`
    - index.html contains `TOO_FEW_VIDEOS`
    - index.html contains `QUOTA_EXCEEDED`
    - index.html contains `YouTube API quota exceeded. Try again after midnight Pacific time.`
    - index.html contains `Something went wrong generating your course. Check the server logs.`
    - index.html contains `Not enough high-quality videos found. Try a broader search term.`
    - index.html contains `Generating...`
    - index.html contains `Generate course`
    - index.html contains `.textContent` (XSS prevention: dynamic content via textContent)
    - index.html contains `es.close()`
    - index.html contains `class="btn-hints"`
    - index.html contains `Reveal thinking points`
    - index.html contains `class="questions-accordion"`
    - index.html contains `Comprehension questions`
    - index.html contains `Module question`
    - index.html contains `Mark as watched`
    - index.html contains `May be outdated`
    - index.html contains `class="score-badge"`
    - index.html contains `class="module-card"`
    - index.html contains `Export course`
    - index.html contains `text/markdown`
    - `node --test tests/unit/frontend.test.js` exits 0
  </acceptance_criteria>
  <done>Form submission opens SSE connection with correct query params, pipeline steps animate with dots, course renders completely with all components (modules, videos, questions, badges, thumbnails), recent searches work, export downloads markdown, error messages display correctly, and all dynamic text uses textContent (no innerHTML XSS).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server API -> DOM | Course JSON data rendered into the DOM — titles, blurbs, questions could contain XSS payloads |
| User input -> URL | Subject string sent as query parameter to SSE endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | renderCourse DOM construction | mitigate | All course JSON strings (titles, blurbs, questions, channel names) set via textContent; createElement used for all dynamic elements |
| T-04-04 | Tampering | Subject input in SSE URL | mitigate | encodeURIComponent applied; server validates subject length <= 200 |
| T-04-05 | Denial of Service | EventSource auto-reconnect | mitigate | es.close() called on course_assembled, error, and onerror events |
</threat_model>

<verification>
1. `node --test tests/unit/frontend.test.js` — all tests green
2. `grep 'new EventSource' index.html` — SSE connection present
3. `grep 'textContent' index.html` — XSS mitigation present
4. `grep 'es.close()' index.html` — EventSource cleanup present
</verification>

<success_criteria>
- Form submit triggers SSE connection with correct query params (subject + skill_level)
- 5 pipeline steps animate in real time (pending -> active -> complete dots)
- Course renders with all components from the JSON contract
- Recent searches persist as pills and pre-fill input on click
- Error messages display correctly for all 3 error types (quota, general, too few videos)
- Export generates and downloads a markdown file
- All dynamic content uses textContent (no innerHTML XSS)
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-persistence-export/04-02-SUMMARY.md`
</output>
