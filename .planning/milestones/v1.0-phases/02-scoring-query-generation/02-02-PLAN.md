---
phase: 02-scoring-query-generation
plan: 02-02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - queries.js
  - tests/unit/queries.test.js
autonomous: true
requirements: [PIPE-02]

must_haves:
  truths:
    - "generateQueries('quantum mechanics', 'beginner') returns an array of 6–8 strings"
    - "generateQueries('quantum mechanics', 'advanced') returns queries with different angle emphasis than beginner"
    - "Result is a plain JavaScript array, not a wrapped object"
    - "generateQueries is wired through callClaude for retry behavior"
    - "Prompt explicitly names angle types and includes a diversity instruction (D-07, D-09)"
  artifacts:
    - path: "queries.js"
      provides: "generateQueries(subject, skillLevel) → string[]"
      exports: ["generateQueries"]
    - path: "tests/unit/queries.test.js"
      provides: "Real tests for PIPE-02 (not just stubs)"
  key_links:
    - from: "queries.js"
      to: "claude.js"
      via: "require('./claude') → callClaude"
      pattern: "callClaude\\(async"
    - from: "queries.js"
      to: "@anthropic-ai/sdk"
      via: "const Anthropic = require('@anthropic-ai/sdk')"
      pattern: "require\\('@anthropic-ai/sdk'\\)"
---

<objective>
Implement queries.js: the generateQueries(subject, skillLevel) function that calls Claude (claude-haiku-4-5-20251001) to produce 6–8 angle-diverse YouTube search queries shaped by the learner's skill level. Update queries.test.js to replace stubs with real assertions using a mocked callClaude.

Purpose: Phase 2 pipeline step 1 — query generation determines what YouTube search results the scorer will evaluate. Angle diversity (D-07) and skill-level emphasis (D-08) are the primary value of this module.
Output: queries.js at project root, tests/unit/queries.test.js with real assertions
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-scoring-query-generation/02-CONTEXT.md
@.planning/phases/02-scoring-query-generation/02-01-SUMMARY.md

<interfaces>
<!-- From claude.js — the callClaude and parseClaudeJSON signatures the executor needs. -->

```javascript
// callClaude(fn, ...args) — retry wrapper
// fn is an async function; all positional args after fn are passed to fn.
// Last arg may be { _testDelayBase: N } for fast test retries (stripped before calling fn).
// Returns: result of fn on success
// Throws: original error after 3 total attempts (1 + 2 retries)
async function callClaude(fn, ...args)

// parseClaudeJSON(text) — strips ```json or ``` fences, then JSON.parse
// Returns: parsed JS value
// Throws: SyntaxError if text (after stripping) is not valid JSON
function parseClaudeJSON(text)

module.exports = { callClaude, parseClaudeJSON };
```

<!-- From @anthropic-ai/sdk — SDK instantiation and messages.create call shape. -->

```javascript
const Anthropic = require('@anthropic-ai/sdk');
const anthropic = new Anthropic(); // reads ANTHROPIC_API_KEY from process.env

const response = await anthropic.messages.create({
  model: 'claude-haiku-4-5-20251001',
  max_tokens: 512,
  messages: [{ role: 'user', content: prompt }]
});
const text = response.content[0].text;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement queries.js with generateQueries</name>
  <files>queries.js</files>
  <read_first>
    - claude.js (callClaude and parseClaudeJSON signatures and behavior — read before writing any call)
    - .planning/phases/02-scoring-query-generation/02-CONTEXT.md (D-07, D-08, D-09, D-10 — locked decisions for the prompt)
    - .planning/phases/02-scoring-query-generation/02-RESEARCH.md (Pattern 1, 2, 3 — SDK instantiation; Pattern: buildQueryPrompt template; Pitfall 3 — Claude may wrap array in object)
    - tests/unit/queries.test.js (current stubs — understand what the test expects)
  </read_first>
  <behavior>
    - generateQueries('topic', 'beginner') calls callClaude once and returns an array
    - generateQueries('topic', 'advanced') returns an array with lecture/deep-dive angle emphasis
    - Result array length is between 6 and 8 inclusive
    - Each element is a non-empty string
    - If Claude returns a wrapped object (not a plain array), generateQueries throws an Error with message "generateQueries: expected array from Claude, got object"
    - Prompt for 'beginner' includes the words "introduction" or "overview" or "tutorial" in the angle hints
    - Prompt for 'advanced' includes the words "lecture" or "deep dive" or "research" in the angle hints
    - Prompt always contains the explicit diversity instruction: "each query must be meaningfully different"
  </behavior>
  <action>
Create `queries.js` at the project root. The file does not exist.

Structure:

1. `'use strict';` at the top.
2. `const Anthropic = require('@anthropic-ai/sdk');` — instantiate at module level.
3. `const { callClaude, parseClaudeJSON } = require('./claude');`
4. `const anthropic = new Anthropic();` — at module level (not inside function; reads ANTHROPIC_API_KEY at startup).
5. `buildQueryPrompt(subject, skillLevel)` — private function, not exported:
   - Uses an `angleHints` object keyed by skill level (D-07, D-08):
     - beginner: 'introduction, overview, tutorial, beginner guide, explained simply, getting started'
     - intermediate: 'tutorial, how-to, practical guide, real-world application, common mistakes'
     - advanced: 'lecture, deep dive, research, advanced concepts, theory, university course'
     - 'all levels': 'overview, tutorial, practical guide, explained, how-to'
   - Fallback: `angleHints[skillLevel] || angleHints['all levels']`
   - Prompt must include the exact phrase "each query must be meaningfully different" (D-09)
   - Prompt must include the exact instruction to return "ONLY a JSON array of query strings" with no wrapper (D-10)
6. `generateQueries(subject, skillLevel)` — exported async function:
   - Calls `callClaude(async () => { ... })` wrapping the anthropic.messages.create call
   - Model: `'claude-haiku-4-5-20251001'` (verified in RESEARCH.md — haiku-3 retires April 19, 2026)
   - max_tokens: 512
   - After callClaude returns the text, call `parseClaudeJSON(text)` to get the parsed value
   - Validate: if result is not an Array, throw `new Error('generateQueries: expected array from Claude, got ' + typeof result)`
   - Return the array
7. `module.exports = { generateQueries };`

Do NOT export buildQueryPrompt, anthropic, or any other internals.
Do NOT add a caching layer to generateQueries — queries vary by subject and level; SSE pipeline generates them fresh each run.
Do NOT use 'claude-3-haiku-20240307' — it retires April 19, 2026.
  </action>
  <verify>
    <automated>node --test --test-concurrency=1 tests/unit/queries.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `queries.js` exists at project root and contains `'use strict'`
    - `queries.js` contains `require('@anthropic-ai/sdk')`
    - `queries.js` contains `require('./claude')`
    - `queries.js` contains `module.exports = { generateQueries }`
    - `queries.js` contains `claude-haiku-4-5-20251001` (correct non-deprecated model)
    - `queries.js` does NOT contain `claude-3-haiku` (deprecated)
    - `queries.js` contains the string `"each query must be meaningfully different"` in the prompt
    - `queries.js` contains `callClaude(async`
    - `queries.js` contains `parseClaudeJSON`
    - `queries.js` contains `if (!Array.isArray(` or equivalent array validation
  </acceptance_criteria>
  <done>queries.js implements generateQueries with angle-diverse prompting, wrapped in callClaude, with array validation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace queries.test.js stubs with real assertions</name>
  <files>tests/unit/queries.test.js</files>
  <read_first>
    - queries.js (just created — understand its exports and structure before writing tests)
    - tests/unit/claude.test.js (reference for how callClaude is tested with injected mock functions)
    - tests/unit/youtube.test.js (reference for how global.fetch is mocked in tests)
    - .planning/phases/02-scoring-query-generation/02-CONTEXT.md (D-07, D-08, D-09, D-10 — behaviors the tests must verify)
    - .planning/phases/02-scoring-query-generation/02-VALIDATION.md (per-task verification map for 02-02-01 and 02-02-02)
  </read_first>
  <behavior>
    - Test "generateQueries returns an array": mock callClaude such that the inner fn receives a mock anthropic; OR mock the anthropic client at module level; the test must confirm the result is an Array
    - Test "result array has 6–8 string elements": mock Claude to return a JSON array of 7 strings; assert length in [6,8] and each element is typeof string
    - Test "beginner and advanced queries are different": inspect the prompt built by the module by capturing the argument passed to anthropic.messages.create; beginner prompt includes 'tutorial' or 'introduction'; advanced prompt includes 'lecture' or 'deep dive'
    - Test "result is plain array, not wrapped object": mock Claude to return '{"queries": ["a", "b"]}'; assert that generateQueries throws (because it receives an object, not an array)
    - Test "throws if Claude returns non-array": same as above — confirms the validation guard works
  </behavior>
  <action>
Rewrite `tests/unit/queries.test.js` fully. Replace all stubs with real assertions.

**Mocking strategy:** The anthropic client is module-level in queries.js. To avoid real API calls, mock the entire `@anthropic-ai/sdk` module by overriding `require.cache` before requiring queries.js, OR use the approach of re-requiring queries.js with a mock injected via module-level mutation. The simplest approach for this codebase (no jest, no dependency injection framework) is to use Node's `mock` from `node:test`:

```javascript
const { test, mock } = require('node:test');
```

Or, simpler: because `callClaude` is a separate module that queries.js requires, stub at the callClaude level by capturing the fn argument and returning a controlled string. This works because callClaude(fn, ...args) calls fn() directly — if we monkeypatch require.cache for claude.js to return a fake callClaude, queries.js will use it.

**Recommended approach (no complex module mocking needed):**
Require queries.js in each test after setting up the environment. Before the require, manipulate `require.cache` to inject a fake `callClaude`:

```javascript
// In each test that needs mock Claude output:
// 1. Delete require.cache entries for claude.js and queries.js
// 2. Inject fake claude.js into require.cache
// 3. Re-require queries.js
// 4. Call generateQueries with fake data
```

This is the cleanest approach without adding test framework dependencies.

**Tests to write:**

1. `generateQueries is a function` — assert typeof generateQueries === 'function'
2. `generateQueries returns an array of strings from Claude response` — inject a callClaude mock that returns `'["query one", "query two", "query three", "query four", "query five", "query six", "query seven"]'`; assert result is Array with length 7; each element is a string
3. `generateQueries throws if Claude returns wrapped object` — inject callClaude mock that returns `'{"queries": ["q1", "q2"]}'`; assert rejects with Error message containing 'expected array'
4. `beginner prompt contains beginner angle keywords` — capture the prompt passed to anthropic.messages.create; assert it includes 'tutorial' or 'introduction' or 'overview'
5. `advanced prompt contains advanced angle keywords` — same mechanism; assert 'lecture' or 'deep dive' or 'research'
6. `prompt always contains diversity instruction` — assert prompt includes 'meaningfully different'

Keep test count at 6–8. Do not over-test. Each test must have a single clear assertion.
  </action>
  <verify>
    <automated>node --test --test-concurrency=1 tests/unit/queries.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/queries.test.js` contains no stub `assert.ok(true, 'stub')` lines (all stubs replaced)
    - `tests/unit/queries.test.js` contains `mock` from `node:test` OR `require.cache` manipulation for mocking
    - `node --test --test-concurrency=1 tests/unit/queries.test.js` exits with 0 (all tests pass)
    - Tests cover: array return shape, 6–8 string elements, non-array rejection, beginner vs advanced angle keywords, diversity instruction in prompt
    - `npm test` exits with 0 (full suite still green, no regressions in other test files)
  </acceptance_criteria>
  <done>queries.test.js has real assertions, all pass, full npm test suite is green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → Claude prompt | subject and skill_level from req.query flow into the prompt string — not yet validated at this layer (validation happens in Plan 04 in server/sse layer) |
| Claude response → JSON.parse | parseClaudeJSON handles fence stripping; generateQueries validates array shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | generateQueries prompt | accept | Personal tool; no adversarial users; basic shape validation guards against malformed Claude output |
| T-02-02-02 | Information Disclosure | ANTHROPIC_API_KEY | accept | Key loaded from .env via dotenv in server.js; never logged; test sets 'test-key' only in test env |
| T-02-02-03 | Denial of Service | Claude API timeout/failure | mitigate | callClaude provides retry with exponential backoff (max 2 retries); failure propagates to SSE error event in Plan 04 |
</threat_model>

<verification>
```bash
node --test --test-concurrency=1 tests/unit/queries.test.js
npm test
```

Both must exit with 0.

Manual smoke check (optional, requires ANTHROPIC_API_KEY in .env):
```javascript
// node -e "require('dotenv').config(); require('./queries').generateQueries('machine learning', 'beginner').then(console.log)"
```
Should print an array of 6–8 strings.
</verification>

<success_criteria>
- queries.js exists with generateQueries exported
- generateQueries uses claude-haiku-4-5-20251001 (not deprecated haiku-3)
- Prompt includes angle hints for skill level and explicit diversity instruction
- Result validated as Array before returning; throws on non-array Claude output
- All tests in queries.test.js pass (no stubs remaining)
- npm test exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-scoring-query-generation/02-02-SUMMARY.md`
</output>
