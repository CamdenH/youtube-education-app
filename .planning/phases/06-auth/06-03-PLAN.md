---
phase: 06-auth
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - landing.html
  - onboarding.html
  - index.html
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-05]
must_haves:
  truths:
    - "A new visitor can sign up via the landing page CTA and Clerk hosted sign-up UI"
    - "After sign-up, user is redirected to /onboarding which explains course generation"
    - "Onboarding page has four content sections: how it works, skill levels, static example, CTA to /app"
    - "The app page (index.html) shows a header bar with Clerk user-button for sign-in/sign-out"
    - "Landing page is fully static — no session detection, no JS-rendered content"
  artifacts:
    - path: "landing.html"
      provides: "Static marketing landing page at /"
      contains: "Sign up free"
    - path: "onboarding.html"
      provides: "Post-signup onboarding page at /onboarding"
      contains: "How it works"
    - path: "index.html"
      provides: "Updated app page with Clerk header bar"
      contains: "clerk-user-button"
  key_links:
    - from: "landing.html"
      to: "/app"
      via: "secondary link href"
      pattern: "href=\"/app\""
    - from: "onboarding.html"
      to: "/app"
      via: "CTA button href"
      pattern: "href=\"/app\""
    - from: "index.html"
      to: "Clerk JS CDN"
      via: "script tag with data-clerk-publishable-key"
      pattern: "data-clerk-publishable-key"
---

<objective>
Create the three HTML pages needed for the auth flow: a marketing landing page (landing.html), a post-signup onboarding page (onboarding.html), and update the existing app (index.html) with a header bar containing the Clerk user-button component.

Purpose: These pages form the user-facing auth experience. The landing page drives sign-ups, onboarding orients new users, and the header bar provides sign-in/out control in the app.

Output: landing.html (new), onboarding.html (new), index.html (updated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-auth/06-CONTEXT.md
@.planning/phases/06-auth/06-RESEARCH.md
@.planning/phases/06-auth/06-UI-SPEC.md

@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create landing.html — static marketing page served at /</name>
  <files>landing.html</files>
  <read_first>
    - index.html (must copy the :root CSS custom properties block exactly for design consistency)
    - .planning/phases/06-auth/06-UI-SPEC.md (Landing Page component spec, Copywriting Contract, Interaction States)
    - .planning/phases/06-auth/06-CONTEXT.md (D-05: fully static, no session detection)
  </read_first>
  <action>
    Create `landing.html` at project root. This is a fully static HTML file with NO JavaScript (per D-05: no session detection). It must be valid for SEO — all content is in the HTML source.

    Structure (from UI-SPEC):
    - Page sections: header (nav) -> hero section -> features section -> CTA section -> footer
    - Max content width: 640px centered (matches existing `main` constraint in index.html)

    CSS: Copy the FULL `:root` block from index.html (all --space-*, --font-size-*, --font-weight-*, --line-height-*, --color-* tokens). Include the same `*, *::before, *::after { box-sizing: border-box; }` and body base styles.

    New buttons MUST use `padding: 8px 16px` (sm/md tokens per UI-SPEC spacing exception — NOT 10px 20px).

    Exact copy from Copywriting Contract:
    - Hero headline: "Learn anything with the best YouTube has to offer"
    - Hero subheading: "Enter a topic. Get a structured course from the top-rated YouTube videos — scored, sequenced, and ready to watch."
    - Primary CTA button: "Sign up free" — links to `/app` (Clerk requireAuth will redirect to sign-up/sign-in)
    - Secondary link: "Go to app" — plain text link to `/app`, color `var(--color-accent)`, no button chrome

    Primary CTA button styling:
    - Background: `var(--color-accent)` (#3b82f6)
    - Text: white
    - Border-radius: 6px
    - Padding: 8px 16px
    - Hover: `opacity: 0.9`
    - Focus: `outline: 2px solid var(--color-accent); outline-offset: 2px`
    - Display as `<a>` tag linking to `/app`, styled as a button (not a form button)

    Features section: 3 items maximum, each with Heading-size label (20px semibold) + Body-size description (16px regular). Feature items to include:
    1. "AI-Powered Curation" / "Claude scores every video on relevance, depth, and production quality — no more guessing which tutorial is worth your time."
    2. "Structured Learning Path" / "Videos are sequenced from foundational to advanced, with comprehension questions to test your understanding."
    3. "Any Topic, Any Level" / "From Python basics to quantum computing — enter any subject and get a course tailored to your skill level."

    Footer: minimal — label size (14px), muted text color, "YouTube Learning Curator" only.

    Header nav: "YouTube Learning Curator" on left (label size, muted), no other nav items. No Clerk JS. No JavaScript at all.

    The `<a href="/app">` links work because server.js (from Plan 02) serves /app with requireAuth — unauthenticated users will be redirected to Clerk's hosted sign-in/sign-up, which then redirects back to /app (or /onboarding for new signups via afterSignUpUrl).
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const html = fs.readFileSync('landing.html', 'utf8'); const checks = ['Sign up free', 'Go to app', 'Learn anything', 'href=\"/app\"', '--color-accent', '--color-bg', 'YouTube Learning Curator']; const pass = checks.every(c => html.includes(c)); console.log(pass ? 'PASS' : 'FAIL: missing ' + checks.filter(c => !html.includes(c)).join(', ')); process.exit(pass ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - landing.html exists at project root
    - landing.html contains NO `<script>` tags (fully static per D-05)
    - landing.html contains the :root CSS block with --color-bg, --color-surface, --color-accent tokens
    - landing.html contains "Learn anything with the best YouTube has to offer" (exact hero headline)
    - landing.html contains "Sign up free" (exact CTA text)
    - landing.html contains "Go to app" (exact secondary link text)
    - landing.html contains `href="/app"` on both the CTA and secondary link
    - landing.html contains `padding: 8px 16px` on the CTA button (NOT 10px 20px)
    - landing.html contains three feature items with headings
    - landing.html contains `<meta name="viewport"` for mobile responsiveness
  </acceptance_criteria>
  <done>landing.html is a fully static HTML page with hero, features, CTA, and footer. No JavaScript. All copy matches the Copywriting Contract. Design tokens match index.html.</done>
</task>

<task type="auto">
  <name>Task 2: Create onboarding.html — post-signup explanation page</name>
  <files>onboarding.html</files>
  <read_first>
    - index.html (must copy :root CSS custom properties block)
    - .planning/phases/06-auth/06-UI-SPEC.md (Onboarding Page component spec, Copywriting Contract)
    - .planning/phases/06-auth/06-CONTEXT.md (D-01, D-02, D-03: four content sections)
  </read_first>
  <action>
    Create `onboarding.html` at project root. This is a fully static HTML file. Per D-01, users arrive here via Clerk's `afterSignUpUrl` redirect after first signup. Per D-02, it's a separate static file.

    CSS: Copy the FULL `:root` block from index.html. Same base styles as landing.html.

    Page title (Heading): "How it works" (from Copywriting Contract)

    Four content sections (per D-03), single scrolling page (no wizard):

    **Section 1 — How course generation works:**
    - Heading: "How course generation works"
    - Body text: "Enter any topic you want to learn. Our AI searches YouTube, scores videos on relevance, depth, and quality, then assembles them into a structured course — from foundational concepts to advanced material."

    **Section 2 — Skill level options:**
    - Heading: "Choose your skill level"
    - List of four items, each with label (bold) + description (from Copywriting Contract):
      - "Beginner" — "New to the topic — starts from foundational concepts"
      - "Intermediate" — "Familiar with the basics — skips intro content"
      - "Advanced" — "Deep expertise — focuses on advanced material"
      - "All levels" — "Mix of everything — good for a broad overview"

    **Section 3 — Static example of a finished course:**
    - Heading: "What you'll get"
    - A card with `--color-surface` background showing a mock course:
      - Course title: "Introduction to Machine Learning" (static example)
      - 3 mock video items inside `--color-surface-raised` sub-cards, each with:
        - Video title (e.g., "But what is a neural network?" / "Gradient descent, how neural networks learn" / "Machine Learning for Everybody")
        - Channel name in muted text (e.g., "3Blue1Brown" / "3Blue1Brown" / "freeCodeCamp")
        - A score badge like "9.2" in accent color
      - This is entirely static HTML — no JavaScript, no API calls

    **Section 4 — CTA:**
    - Button: "Start learning" (from Copywriting Contract)
    - Links to `/app`
    - Accent background, white text, 6px border-radius, padding: 8px 16px
    - Same hover/focus states as landing.html CTA

    Max content width: 640px centered. No JavaScript needed.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const html = fs.readFileSync('onboarding.html', 'utf8'); const checks = ['How it works', 'Start learning', 'href=\"/app\"', 'Beginner', 'Intermediate', 'Advanced', 'All levels', '--color-surface', 'What you', 'Choose your skill level']; const pass = checks.every(c => html.includes(c)); console.log(pass ? 'PASS' : 'FAIL: missing ' + checks.filter(c => !html.includes(c)).join(', ')); process.exit(pass ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - onboarding.html exists at project root
    - onboarding.html contains the :root CSS block matching index.html tokens
    - onboarding.html contains "How it works" (page title)
    - onboarding.html contains "Start learning" (CTA button text)
    - onboarding.html contains `href="/app"` on the CTA
    - onboarding.html contains all four skill level names: "Beginner", "Intermediate", "Advanced", "All levels"
    - onboarding.html contains all four skill level descriptions from Copywriting Contract
    - onboarding.html contains a static course example card with at least 3 video items
    - onboarding.html contains `padding: 8px 16px` on the CTA (NOT 10px 20px)
    - onboarding.html contains NO `<script>` tags (fully static)
  </acceptance_criteria>
  <done>onboarding.html has four content sections (how it works, skill levels, static example, CTA to /app). All copy matches the Copywriting Contract. Design tokens match index.html. No JavaScript.</done>
</task>

<task type="auto">
  <name>Task 3: Update index.html — add header bar with Clerk user-button</name>
  <files>index.html</files>
  <read_first>
    - index.html (MUST read full current file to understand structure before modifying)
    - .planning/phases/06-auth/06-UI-SPEC.md (Header Bar component spec, Clerk User Button spec)
    - .planning/phases/06-auth/06-RESEARCH.md (Pattern 5: Clerk JS in Vanilla HTML CDN)
  </read_first>
  <action>
    Modify `index.html` to add a fixed header bar at the top and the Clerk JS CDN script.

    **Header bar HTML** — add immediately after `<body>` tag:

    ```html
    <header id="app-header" style="
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      height: 48px;
      background: var(--color-surface);
      border-bottom: 1px solid var(--color-surface-raised);
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 var(--space-md);
      z-index: 1000;
    ">
      <span style="font-size: var(--font-size-label); color: var(--color-text-muted);">YouTube Learning Curator</span>
      <div id="clerk-user-button"></div>
    </header>
    ```

    **Main content offset** — add `padding-top: 48px` to the `main` element (or body/wrapper) to prevent header overlap. Find the existing `main` or primary content container and add the padding.

    **Clerk JS CDN script** — add before closing `</body>`:

    ```html
    <!-- Clerk JS — copy exact script tag from Clerk Dashboard > API Keys > JavaScript -->
    <!-- The data-clerk-publishable-key and src URL must be filled from Dashboard -->
    <script
      defer
      crossorigin="anonymous"
      data-clerk-publishable-key="CLERK_PUBLISHABLE_KEY_PLACEHOLDER"
      src="https://CLERK_FAPI_URL_PLACEHOLDER/npm/@clerk/clerk-js@6/dist/clerk.browser.js"
      type="text/javascript"
    ></script>

    <script>
      window.addEventListener('load', async function () {
        await Clerk.load();
        if (Clerk.user) {
          Clerk.mountUserButton(document.getElementById('clerk-user-button'));
        }
      });
    </script>
    ```

    IMPORTANT: The `data-clerk-publishable-key` and `src` URL contain placeholder values. The user must replace them with values from the Clerk Dashboard (per Pitfall 3 in RESEARCH.md — do not construct the URL manually; copy from Dashboard). Add an HTML comment above the script tag instructing the user to replace these.

    Do NOT modify any existing functionality in index.html — the form, course display, video cards, etc. remain unchanged. Only add the header bar and Clerk script.

    Do NOT override Clerk component styles — the UserButton handles its own appearance (per UI-SPEC: "Do NOT override Clerk component styles").
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const html = fs.readFileSync('index.html', 'utf8'); const checks = ['app-header', 'clerk-user-button', 'data-clerk-publishable-key', 'Clerk.mountUserButton', 'Clerk.load()', 'padding-top: 48px', 'YouTube Learning Curator']; const pass = checks.every(c => html.includes(c)); console.log(pass ? 'PASS' : 'FAIL: missing ' + checks.filter(c => !html.includes(c)).join(', ')); process.exit(pass ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `<header id="app-header"` with height 48px, background var(--color-surface)
    - index.html contains `<div id="clerk-user-button"></div>` inside the header
    - index.html contains "YouTube Learning Curator" text in the header left slot
    - index.html contains `data-clerk-publishable-key` in a script tag
    - index.html contains `Clerk.load()` and `Clerk.mountUserButton`
    - index.html contains `padding-top: 48px` on the main content area
    - index.html still contains all existing form elements (#subject-input, #btn-generate, etc.) — no existing functionality removed
    - index.html contains an HTML comment instructing user to replace Clerk placeholder values from Dashboard
  </acceptance_criteria>
  <done>index.html has a fixed 48px header with app name and Clerk user-button. Clerk JS CDN script loads and mounts the UserButton for signed-in users. Existing app functionality is untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify auth flow end-to-end</name>
  <files>landing.html, onboarding.html, index.html</files>
  <action>
    Human verification of the complete auth flow. No automated action — this checkpoint verifies the visual and functional integration of all Phase 6 components working together with live Clerk and Supabase services.
  </action>
  <what-built>
    Complete Phase 6 auth flow: landing page, onboarding page, protected app with Clerk user-button, webhook syncing users to Supabase.
  </what-built>
  <how-to-verify>
    Prerequisites: Clerk account configured, Supabase users table created, env vars set, server running (`npm start`).

    1. Visit http://localhost:3000/ — should see the landing page with "Learn anything..." headline and "Sign up free" CTA
    2. Click "Sign up free" — should redirect to Clerk hosted sign-up (via /app -> requireAuth redirect)
    3. Create an account — should redirect to /onboarding after signup
    4. Verify onboarding page shows all 4 sections: how it works, skill levels, example course, "Start learning" button
    5. Click "Start learning" — should land on /app with the header bar showing the Clerk user-button
    6. Click the Clerk user-button — should show sign-out option
    7. Sign out — should redirect to Clerk sign-in page (via /app requireAuth)
    8. Check Supabase Dashboard -> Table Editor -> users — new row should exist with your clerk_id and plan='free'
    9. Open a new incognito window, visit http://localhost:3000/api/course-stream?subject=test&skill_level=beginner — should return 401 JSON
  </how-to-verify>
  <verify>Manual verification — see how-to-verify steps above</verify>
  <done>All 9 verification steps pass. Auth flow works end-to-end: landing -> sign-up -> onboarding -> app -> sign-out. Webhook creates user in Supabase. Unauth API returns 401.</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Landing page -> /app | Public page links to auth-gated app; Clerk handles auth UI |
| Browser -> Clerk JS CDN | Third-party script loaded in browser; publishable key is intentionally public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Information Disclosure | landing.html | accept | Fully static HTML with no secrets; publishable key is intentionally public per Clerk design |
| T-06-11 | Spoofing | index.html Clerk JS | mitigate | Clerk JS loaded from official CDN with crossorigin attribute; publishable key used for identification only, not authentication |
| T-06-12 | Tampering | onboarding.html | accept | Static HTML, no user input, no form submission; no attack surface |
| T-06-13 | Information Disclosure | index.html | mitigate | Clerk user-button only renders for signed-in users (Clerk.user check); no user data exposed to unauthenticated visitors |
</threat_model>

<verification>
- Landing page: `node -e "..."` check (see Task 1 verify)
- Onboarding page: `node -e "..."` check (see Task 2 verify)
- index.html header: `node -e "..."` check (see Task 3 verify)
- Visual/functional: Task 4 checkpoint covers end-to-end flow
</verification>

<success_criteria>
- landing.html at / shows marketing page with sign-up CTA — no JavaScript, SEO-ready
- onboarding.html at /onboarding shows 4 explanation sections — accessible after first signup
- index.html header bar shows Clerk user-button for signed-in users
- End-to-end flow works: landing -> sign-up -> onboarding -> app with header -> sign-out
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth/06-03-SUMMARY.md`
</output>
