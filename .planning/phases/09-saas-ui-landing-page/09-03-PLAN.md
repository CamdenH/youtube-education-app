---
phase: 09-saas-ui-landing-page
plan: 03
type: execute
wave: 2
depends_on:
  - 09-02
files_modified:
  - landing.html
autonomous: true
requirements:
  - D-05
  - D-06
  - D-07

must_haves:
  truths:
    - "landing.html nav shows Pricing link and Sign up free link"
    - "Hero CTAs link to Clerk sign-up and sign-in URLs, not /app"
    - "Features list replaced with 3-step how-it-works section"
    - "Sample course preview section present with ML course static mockup"
    - "Bottom CTA links to Clerk sign-up URL"
  artifacts:
    - path: "landing.html"
      provides: "Updated marketing landing page"
      contains: "how-it-works"
  key_links:
    - from: "landing.html nav"
      to: "/pricing"
      via: "nav-link anchor"
      pattern: "href=\"/pricing\""
    - from: "landing.html hero CTA"
      to: "https://accounts.comoedu.com/sign-up"
      via: "btn-primary anchor"
      pattern: "href=\"https://accounts.comoedu.com/sign-up\""
    - from: "landing.html .sample-preview"
      to: "static ML course data"
      via: "inline HTML video-list"
      pattern: "3Blue1Brown"
---

<objective>
Update landing.html with the full set of changes specified in PATTERNS.md and UI-SPEC: updated nav (with Pricing + Sign up free links), corrected hero CTA hrefs, the "How it works" 3-step section replacing the features list, and the sample course preview section. All CSS and copy from UI-SPEC Copywriting Contract verbatim.

Purpose: Landing page is the conversion surface for the SaaS product (D-05, D-06, D-07).
Output: Updated landing.html at project root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-saas-ui-landing-page/09-PATTERNS.md
@.planning/phases/09-saas-ui-landing-page/09-UI-SPEC.md
@.planning/phases/09-saas-ui-landing-page/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update landing.html head — title, meta description, nav CSS</name>
  <files>landing.html</files>
  <read_first>
    - landing.html (full file — 271 lines; understand current structure before any edits; locate exact lines for title, meta description, header block, hero CTAs, features section, CTA section)
  </read_first>
  <action>
    Make the following changes to landing.html. Read the full file first to confirm line numbers match expectations.

    CHANGE 1 — Update `<title>` (currently reversed order):
    FROM: whatever the current title is
    TO: `<title>Learn anything with the best YouTube has to offer — YouTube Learning Curator</title>`

    CHANGE 2 — Update `<meta name="description">`:
    TO: `<meta name="description" content="Enter a topic. Get a structured course from the top-rated YouTube videos — scored, sequenced, and ready to watch.">`

    CHANGE 3 — Add nav CSS block. Insert the following CSS block AFTER the existing `.link-secondary:focus` rule and BEFORE the existing `/* ── Features ── */` comment (or wherever the features CSS block starts):

    ```css
    /* ── Nav ───────────────────────────────────────────────────────────────── */

    .nav {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: var(--space-lg) 0 var(--space-xl);
    }

    .nav-links {
      display: flex;
      gap: var(--space-md);
      align-items: center;
    }

    .nav-link {
      font-size: var(--font-size-body);
      color: var(--color-text-muted);
      text-decoration: none;
    }

    .nav-link:hover {
      color: var(--color-text);
    }

    .nav-link:focus {
      outline: 2px solid var(--color-accent);
      outline-offset: 2px;
    }
    ```

    CHANGE 4 — Replace the features CSS block (`.features`, `.features-list`, `.feature-item`, `.feature-heading`, `.feature-description`) with the how-it-works + sample-preview CSS. Remove ALL of the features CSS classes and replace with:

    ```css
    /* ── How it works ──────────────────────────────────────────────────────── */

    .how-it-works {
      padding: var(--space-xl) 0;
      border-top: 1px solid var(--color-surface-raised);
    }

    .how-it-works-heading {
      font-size: var(--font-size-heading);
      font-weight: var(--font-weight-semibold);
      line-height: var(--line-height-heading);
      color: var(--color-text);
      margin: 0 0 var(--space-xl) 0;
    }

    .steps-list {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: var(--space-xl);
    }

    .step-item {
      display: flex;
      flex-direction: column;
      gap: var(--space-xs);
    }

    .step-number {
      font-size: var(--font-size-label);
      font-weight: var(--font-weight-semibold);
      color: var(--color-accent);
      margin: 0;
      line-height: var(--line-height-label);
    }

    .step-heading {
      font-size: var(--font-size-heading);
      font-weight: var(--font-weight-semibold);
      line-height: var(--line-height-heading);
      color: var(--color-text);
      margin: 0;
    }

    .step-description {
      font-size: var(--font-size-body);
      color: var(--color-text-muted);
      line-height: var(--line-height-body);
      margin: 0;
    }

    /* ── Sample course preview ──────────────────────────────────────────────── */

    .sample-preview {
      padding: var(--space-xl) 0;
      border-top: 1px solid var(--color-surface-raised);
    }

    .sample-preview-heading {
      font-size: var(--font-size-heading);
      font-weight: var(--font-weight-semibold);
      line-height: var(--line-height-heading);
      color: var(--color-text);
      margin: 0 0 var(--space-md) 0;
    }

    .preview-card {
      background: var(--color-surface);
      border-radius: 8px;
      padding: var(--space-md);
      border: 1px solid var(--color-surface-raised);
    }

    .preview-course-title {
      font-size: var(--font-size-body);
      font-weight: var(--font-weight-semibold);
      color: var(--color-text);
      margin: 0 0 var(--space-xs) 0;
    }

    .module-heading {
      font-size: var(--font-size-label);
      color: var(--color-text-muted);
      margin: 0 0 var(--space-md) 0;
    }

    .video-list {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: var(--space-sm);
    }

    .video-item {
      background: var(--color-surface-raised);
      border-radius: 6px;
      padding: var(--space-sm) var(--space-md);
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: var(--space-sm);
    }

    .video-info {
      display: flex;
      flex-direction: column;
      gap: 2px;
      flex: 1;
      min-width: 0;
    }

    .video-title {
      font-size: var(--font-size-body);
      color: var(--color-text);
      margin: 0;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .video-channel {
      font-size: var(--font-size-label);
      color: var(--color-text-muted);
      margin: 0;
    }

    .score-badge {
      font-size: var(--font-size-label);
      font-weight: var(--font-weight-semibold);
      color: var(--color-accent);
      white-space: nowrap;
      flex-shrink: 0;
    }
    ```
  </action>
  <verify>
    node --test --test-concurrency=1 tests/unit/server.test.js
  </verify>
  <acceptance_criteria>
    - landing.html contains `Learn anything with the best YouTube has to offer — YouTube Learning Curator` in the title tag
    - landing.html contains `class="how-it-works"`
    - landing.html contains `class="steps-list"`
    - landing.html contains `class="sample-preview"`
    - landing.html contains `class="preview-card"`
    - landing.html does NOT contain `class="features-list"` (old class removed)
    - landing.html does NOT contain `class="feature-item"` (old class removed)
    - node --test exits 0 (no regressions)
  </acceptance_criteria>
  <done>landing.html head section updated: title, meta description, nav CSS, how-it-works CSS, sample-preview CSS.</done>
</task>

<task type="auto">
  <name>Task 2: Update landing.html body — nav HTML, hero CTAs, how-it-works section, sample preview, bottom CTA</name>
  <files>landing.html</files>
  <read_first>
    - landing.html (after Task 1 edits — read full file to locate current body structure: header block ~line 222, hero CTAs ~lines 232-234, features section ~lines 237-253, CTA section ~lines 255-262)
  </read_first>
  <action>
    CHANGE 1 — Replace the existing `<header>` block. Current header is:
    ```html
    <header>
      <span class="nav-brand">YouTube Learning Curator</span>
    </header>
    ```
    Replace with:
    ```html
    <header>
      <nav class="nav">
        <span class="nav-brand">YouTube Learning Curator</span>
        <div class="nav-links">
          <a href="/pricing" class="nav-link">Pricing</a>
          <a href="https://accounts.comoedu.com/sign-up" class="btn-primary">Sign up free</a>
        </div>
      </nav>
    </header>
    ```

    CHANGE 2 — Update hero CTA hrefs and secondary link text. Find the hero-actions block containing:
    ```html
    <a href="/app" class="btn-primary">Sign up free</a>
    <a href="/app" class="link-secondary">Go to app</a>
    ```
    Replace with:
    ```html
    <a href="https://accounts.comoedu.com/sign-up" class="btn-primary">Sign up free</a>
    <a href="https://accounts.comoedu.com/sign-in" class="link-secondary">Sign in</a>
    ```

    CHANGE 3 — Also add aria-label to the hero section if it does not already have one:
    `<section class="hero" aria-label="Hero">`

    CHANGE 4 — Replace the entire features section. Find the section that contains `.features` / `.features-list` (the block between the hero section and the CTA section). Remove it entirely and replace with the following two sections:

    ```html
    <!-- How it works section -->
    <section class="how-it-works" aria-label="How it works">
      <h2 class="how-it-works-heading">How it works</h2>
      <ol class="steps-list">
        <li class="step-item">
          <p class="step-number">Step 1</p>
          <h3 class="step-heading">Enter your topic</h3>
          <p class="step-description">Tell us what you want to learn and your skill level — beginner, intermediate, advanced, or all levels.</p>
        </li>
        <li class="step-item">
          <p class="step-number">Step 2</p>
          <h3 class="step-heading">AI curates the best videos</h3>
          <p class="step-description">We search YouTube and score every video on relevance, depth, channel credibility, and production quality. Only the top 12 make it in.</p>
        </li>
        <li class="step-item">
          <p class="step-number">Step 3</p>
          <h3 class="step-heading">Get a structured course</h3>
          <p class="step-description">Videos are organized into 3–4 thematic modules with sequenced learning, comprehension questions, and hints to help it stick.</p>
        </li>
      </ol>
    </section>

    <!-- Sample course preview -->
    <section class="sample-preview" aria-label="Sample course">
      <h2 class="sample-preview-heading">What you get</h2>
      <div class="preview-card">
        <p class="preview-course-title">Introduction to Machine Learning</p>
        <p class="module-heading">Module 1: Foundations</p>
        <ul class="video-list">
          <li class="video-item">
            <div class="video-info">
              <p class="video-title">But what is a neural network?</p>
              <p class="video-channel">3Blue1Brown</p>
            </div>
            <span class="score-badge">9.2</span>
          </li>
          <li class="video-item">
            <div class="video-info">
              <p class="video-title">Gradient descent, how neural networks learn</p>
              <p class="video-channel">3Blue1Brown</p>
            </div>
            <span class="score-badge">9.0</span>
          </li>
          <li class="video-item">
            <div class="video-info">
              <p class="video-title">Machine Learning for Everybody</p>
              <p class="video-channel">freeCodeCamp.org</p>
            </div>
            <span class="score-badge">8.7</span>
          </li>
        </ul>
      </div>
    </section>
    ```

    CHANGE 5 — Update the bottom CTA section heading and button href. Find the cta-section block. The heading must read "Ready to start learning?" and the button must link to Clerk sign-up:
    - Change heading to: `<h2 class="cta-heading">Ready to start learning?</h2>`
    - Change CTA button: `<a href="https://accounts.comoedu.com/sign-up" class="btn-primary">Sign up free</a>`
    - Remove the secondary "Go to app" link in the CTA section if present (per PATTERNS.md — CTA section has only the primary button)

    CHANGE 6 — Add aria-label to cta-section if absent:
    `<section class="cta-section" aria-label="Call to action">`
  </action>
  <verify>
    node --test --test-concurrency=1 tests/unit/server.test.js

    Also verify structure directly:
    grep -c "class=\"how-it-works\"" landing.html   # must output 1
    grep -c "class=\"sample-preview\"" landing.html  # must output 1
    grep -c "3Blue1Brown" landing.html               # must output 2 (two video items)
    grep "href=\"/app\"" landing.html                # must output nothing (all /app hrefs replaced)
  </verify>
  <acceptance_criteria>
    - landing.html contains `href="/pricing"` in the nav
    - landing.html contains `href="https://accounts.comoedu.com/sign-up"` (at least 3 occurrences: nav CTA, hero CTA, bottom CTA)
    - landing.html contains `href="https://accounts.comoedu.com/sign-in"` (hero secondary link)
    - landing.html contains `class="how-it-works"`
    - landing.html contains `Step 1` and `Step 2` and `Step 3`
    - landing.html contains `Enter your topic`
    - landing.html contains `AI curates the best videos`
    - landing.html contains `Get a structured course`
    - landing.html contains `class="sample-preview"`
    - landing.html contains `3Blue1Brown`
    - landing.html contains `freeCodeCamp.org`
    - landing.html contains `Ready to start learning?`
    - landing.html does NOT contain `href="/app"` (all /app links removed from landing page)
    - node --test exits 0 (no regressions)
  </acceptance_criteria>
  <done>landing.html body fully updated: nav with Pricing/Sign up links, hero CTAs corrected, how-it-works 3-step section, sample preview ML course mockup, bottom CTA updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → landing.html | Static marketing page; no user input, no auth state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | External href links in landing.html | accept | All hrefs are hardcoded to known domains (accounts.comoedu.com, /pricing); no user-controlled redirect targets |
| T-09-03-02 | Information Disclosure | Sample course preview content | accept | Static mockup data only — no real user data, no server secrets exposed |
</threat_model>

<verification>
node --test --test-concurrency=1 tests/unit/server.test.js

grep "class=\"how-it-works\"" landing.html
grep "class=\"sample-preview\"" landing.html
grep "href=\"/pricing\"" landing.html
grep "href=\"https://accounts.comoedu.com/sign-up\"" landing.html
</verification>

<success_criteria>
- landing.html nav shows "Pricing" link + "Sign up free" button
- Hero CTA links to accounts.comoedu.com/sign-up (not /app)
- Hero secondary link text is "Sign in" linking to accounts.comoedu.com/sign-in
- 3-step how-it-works section present with correct copy from UI-SPEC
- Sample preview shows Introduction to Machine Learning with 3 videos
- Bottom CTA links to accounts.comoedu.com/sign-up with heading "Ready to start learning?"
- No href="/app" anywhere in landing.html
- All server tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-saas-ui-landing-page/09-03-SUMMARY.md`
</output>
