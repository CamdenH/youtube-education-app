---
phase: 09-saas-ui-landing-page
plan: 05
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
  - 09-04
files_modified:
  - onboarding.html
autonomous: true
requirements:
  - D-08
  - D-09
  - D-10

must_haves:
  truths:
    - "onboarding.html has welcome heading Welcome to YouTube Learning Curator"
    - "onboarding.html has 3 onboarding bullet items with step-heading and step-body"
    - "onboarding.html mentions the free tier limit with upgrade link to /pricing"
    - "onboarding.html Start learning CTA links to /app"
    - "onboarding.html nav shows Go to app link"
    - "GET /onboarding authenticated returns 200 test is GREEN (final Wave 0 test)"
    - "Full test suite is 183+ tests, all green"
  artifacts:
    - path: "onboarding.html"
      provides: "Post-signup onboarding page"
      contains: "Welcome to YouTube Learning Curator"
  key_links:
    - from: "onboarding.html tier-notice"
      to: "/pricing"
      via: "anchor in tier-notice paragraph"
      pattern: "href=\"/pricing\""
    - from: "onboarding.html CTA"
      to: "/app"
      via: "btn-primary anchor"
      pattern: "href=\"/app\".*Start learning"
---

<objective>
Rebuild onboarding.html body content. The existing file (304 lines) is a "How it works" app documentation page — the entire body must be replaced with the welcome/onboarding structure: nav, welcome heading, 3 bullet items, tier notice with upgrade link, and Start learning CTA. The CSS block is also rebuilt — old classes removed, new onboarding classes added.

After this plan, the final Wave 0 test "GET /onboarding authenticated returns 200" becomes GREEN because onboarding.html now exists and is served correctly.

Purpose: Post-signup welcome experience (D-08, D-09, D-10).
Output: Rebuilt onboarding.html at project root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-saas-ui-landing-page/09-PATTERNS.md
@.planning/phases/09-saas-ui-landing-page/09-UI-SPEC.md
@.planning/phases/09-saas-ui-landing-page/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild onboarding.html — CSS section</name>
  <files>onboarding.html</files>
  <read_first>
    - onboarding.html (full file — 304 lines; read carefully before touching anything; understand exact current CSS block structure, identify the `:root` block lines 10-38 that MUST be preserved, and the `*, body` reset rules that MUST be preserved)
  </read_first>
  <action>
    The file rebuild is precise. The `:root` block (lines 10-38), `*, *::before, *::after` reset, and `body` rule must be preserved exactly. Everything else in the `<style>` block is replaced.

    PRESERVE verbatim:
    - `<html lang="en">`, `<head>`, charset/viewport/color-scheme meta tags
    - The existing `:root` block with all CSS variables (lines 10-38)
    - `*, *::before, *::after { box-sizing: border-box; }` reset
    - `body { ... }` rule

    REMOVE entirely (these CSS classes are not used in the new structure):
    - `.page-header`, `.page-title` CSS
    - `.content-section`, `.section-heading`, `.section-body` CSS
    - `.skill-list`, `.skill-item`, `.skill-label`, `.skill-description` CSS
    - `.example-card`, `.example-course-title`, `.video-list`, `.video-item`, `.video-info`, `.video-title`, `.video-channel`, `.score-badge` CSS

    ADD these new CSS blocks after the `body` rule (replacing everything removed above):

    ```css
    /* ── Page wrapper ───────────────────────────────────────────────────────── */

    .page-wrapper {
      max-width: 640px;
      margin: 0 auto;
      padding: var(--space-lg);
    }

    /* ── Nav ───────────────────────────────────────────────────────────────── */

    .nav {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: var(--space-lg) 0 var(--space-xl);
    }

    .nav-brand {
      font-size: var(--font-size-label);
      color: var(--color-text-muted);
      line-height: var(--line-height-label);
    }

    .nav-links {
      display: flex;
      gap: var(--space-md);
      align-items: center;
    }

    .nav-link {
      font-size: var(--font-size-body);
      color: var(--color-text-muted);
      text-decoration: none;
    }

    .nav-link:hover {
      color: var(--color-text);
    }

    .nav-link:focus {
      outline: 2px solid var(--color-accent);
      outline-offset: 2px;
    }

    /* ── Welcome heading ────────────────────────────────────────────────────── */

    .hero-headline {
      font-size: var(--font-size-display);
      font-weight: var(--font-weight-semibold);
      line-height: var(--line-height-display);
      color: var(--color-text);
      margin: 0 0 var(--space-xl) 0;
    }

    /* ── Onboarding steps ───────────────────────────────────────────────────── */

    .onboarding-steps {
      list-style: none;
      margin: 0 0 var(--space-xl) 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: var(--space-lg);
    }

    .step-item {
      display: flex;
      flex-direction: column;
      gap: var(--space-xs);
    }

    .step-heading {
      font-size: var(--font-size-body);
      font-weight: var(--font-weight-semibold);
      color: var(--color-text);
      margin: 0;
    }

    .step-body {
      font-size: var(--font-size-body);
      color: var(--color-text-muted);
      line-height: var(--line-height-body);
      margin: 0;
    }

    /* ── Tier notice ────────────────────────────────────────────────────────── */

    .tier-notice {
      font-size: var(--font-size-body);
      color: var(--color-text-muted);
      margin: 0 0 var(--space-xl) 0;
    }

    .tier-notice a {
      color: var(--color-accent);
      text-decoration: none;
    }

    .tier-notice a:hover {
      text-decoration: underline;
    }

    /* ── CTA section ────────────────────────────────────────────────────────── */

    .cta-section {
      padding: var(--space-xl) 0 var(--space-2xl);
    }

    /* ── btn-primary ────────────────────────────────────────────────────────── */

    .btn-primary {
      display: inline-block;
      background: var(--color-accent);
      color: white;
      text-decoration: none;
      padding: 8px 16px;
      border-radius: 6px;
      font-size: var(--font-size-body);
      font-family: inherit;
      font-weight: var(--font-weight-semibold);
      line-height: var(--line-height-body);
    }

    .btn-primary:hover {
      opacity: 0.9;
    }

    .btn-primary:focus {
      outline: 2px solid var(--color-accent);
      outline-offset: 2px;
    }

    /* ── Footer ─────────────────────────────────────────────────────────────── */

    footer {
      padding: var(--space-xl) 0 var(--space-lg);
      border-top: 1px solid var(--color-surface-raised);
    }

    .footer-text {
      font-size: var(--font-size-label);
      color: var(--color-text-muted);
      margin: 0;
      line-height: var(--line-height-label);
    }

    @media (max-width: 480px) {
      .hero-headline {
        font-size: 22px;
      }
    }
    ```

    Also update `<title>` and `<meta name="description">`:
    - `<title>Welcome — YouTube Learning Curator</title>`
    - `<meta name="description" content="You're all set. Here's how to get the most out of YouTube Learning Curator.">`
  </action>
  <verify>
    node --check onboarding.html is not applicable (HTML). Instead verify:
    grep "class=\"onboarding-steps\"" onboarding.html   # must output 1
    grep "class=\"tier-notice\"" onboarding.html        # must output 1
    grep "class=\"hero-headline\"" onboarding.html      # must output 1
    grep "class=\"example-card\"" onboarding.html       # must output nothing (old class removed)
    grep "class=\"skill-list\"" onboarding.html         # must output nothing (old class removed)
  </verify>
  <acceptance_criteria>
    - onboarding.html contains `<title>Welcome — YouTube Learning Curator</title>`
    - onboarding.html contains `class="hero-headline"`
    - onboarding.html contains `class="onboarding-steps"`
    - onboarding.html contains `class="tier-notice"`
    - onboarding.html contains `class="cta-section"`
    - onboarding.html does NOT contain `class="example-card"` (old class removed)
    - onboarding.html does NOT contain `class="skill-list"` (old class removed)
    - onboarding.html does NOT contain `class="page-header"` (old class removed)
    - onboarding.html still contains `:root` CSS variables block with `--color-bg: #0f0f0f`
  </acceptance_criteria>
  <done>onboarding.html CSS section rebuilt — old classes removed, new onboarding classes added, :root preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Replace onboarding.html body with welcome content</name>
  <files>onboarding.html</files>
  <read_first>
    - onboarding.html (after Task 1 edits — read current body block to locate the old body content that must be fully replaced)
  </read_first>
  <action>
    Replace the entire `<body>` block (from `<body>` to `</body>`) with the following content verbatim. All copy is from UI-SPEC Copywriting Contract:

    ```html
    <body>
      <div class="page-wrapper">
        <!-- Nav -->
        <header>
          <nav class="nav">
            <span class="nav-brand">YouTube Learning Curator</span>
            <div class="nav-links">
              <a href="/app" class="nav-link">Go to app</a>
            </div>
          </nav>
        </header>

        <!-- Welcome section -->
        <main>
          <section aria-label="Welcome">
            <h1 class="hero-headline">Welcome to YouTube Learning Curator</h1>
            <ul class="onboarding-steps">
              <li class="step-item">
                <p class="step-heading">Enter any topic</p>
                <p class="step-body">Type a subject you want to learn — anything from Python basics to music theory.</p>
              </li>
              <li class="step-item">
                <p class="step-heading">Choose your level</p>
                <p class="step-body">Pick beginner, intermediate, advanced, or all levels. The course adapts to where you are.</p>
              </li>
              <li class="step-item">
                <p class="step-heading">Get your structured course</p>
                <p class="step-body">AI curates the top YouTube videos, organizes them into modules, and generates comprehension questions.</p>
              </li>
            </ul>
            <p class="tier-notice">Your free plan includes 1 course per month. <a href="/pricing">Upgrade to Early Access</a> for 20 courses per month.</p>
            <div class="cta-section">
              <a href="/app" class="btn-primary">Start learning</a>
            </div>
          </section>
        </main>

        <!-- Footer -->
        <footer>
          <p class="footer-text">YouTube Learning Curator</p>
        </footer>
      </div>
    </body>
    ```
  </action>
  <verify>
    node --test --test-concurrency=1 tests/unit/server.test.js

    All 3 Wave 0 tests must now be GREEN:
    - "GET /pricing returns 200 and HTML content" — GREEN (from plan 09-04)
    - "GET /onboarding unauthenticated redirects to /" — GREEN (from plan 09-02)
    - "GET /onboarding authenticated returns 200" — GREEN (onboarding.html now exists and is served by the auth-gated route)

    Also run the full suite:
    node --test --test-concurrency=1 tests/unit/*.test.js

    Expected: 183+ tests passing, 0 failures.

    Additionally verify body content:
    grep "Welcome to YouTube Learning Curator" onboarding.html  # must output 1
    grep "href=\"/pricing\"" onboarding.html                    # must output 1
    grep "Upgrade to Early Access" onboarding.html             # must output 1
    grep "Start learning" onboarding.html                       # must output 1
    grep "href=\"/app\"" onboarding.html                        # must output 2 (nav + CTA)
  </verify>
  <acceptance_criteria>
    - onboarding.html contains `Welcome to YouTube Learning Curator`
    - onboarding.html contains `Enter any topic`
    - onboarding.html contains `Choose your level`
    - onboarding.html contains `Get your structured course`
    - onboarding.html contains `Your free plan includes 1 course per month`
    - onboarding.html contains `href="/pricing"` (upgrade link in tier notice)
    - onboarding.html contains `Upgrade to Early Access`
    - onboarding.html contains `href="/app"` in both the nav ("Go to app") and the CTA ("Start learning")
    - onboarding.html contains `Start learning`
    - onboarding.html contains `Go to app`
    - onboarding.html does NOT contain `How it works` as a heading (old body content removed)
    - node --test --test-concurrency=1 tests/unit/server.test.js exits 0 (all 3 Wave 0 tests GREEN)
    - node --test --test-concurrency=1 tests/unit/*.test.js exits 0 (full suite green, 183+ tests)
  </acceptance_criteria>
  <done>onboarding.html body rebuilt with welcome heading, 3 bullets, tier notice, Start learning CTA. All Wave 0 tests GREEN. Full suite 183+ tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → onboarding.html | Auth-gated by server — only authenticated users receive this page (D-10) |
| onboarding.html → /app | CTA navigates authenticated user to the app |
| onboarding.html → /pricing | Tier notice link navigates to public pricing page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Elevation of Privilege | onboarding.html access | mitigate | Page is served only after getAuth() confirms userId in server.js — unauthenticated users receive 302 to / (D-10, D-03). No auth bypass possible via direct file access because express.static is registered after the explicit /onboarding route. |
| T-09-05-02 | Information Disclosure | Tier plan details in onboarding copy | accept | Tier limits (1/month free, 20/month paid) are public knowledge — same data shown on pricing.html |
</threat_model>

<verification>
node --test --test-concurrency=1 tests/unit/server.test.js
node --test --test-concurrency=1 tests/unit/*.test.js

All 3 Wave 0 tests GREEN. Full suite 183+ tests passing, 0 failures.
</verification>

<success_criteria>
- onboarding.html body fully replaced with welcome content
- Welcome heading "Welcome to YouTube Learning Curator" present
- 3 bullet items with exact copy from UI-SPEC
- Tier notice with link to /pricing and "Upgrade to Early Access" text
- "Start learning" CTA links to /app
- Nav shows "Go to app" link
- All 3 Wave 0 tests GREEN
- Full test suite 183+ tests, 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/09-saas-ui-landing-page/09-05-SUMMARY.md`
</output>
