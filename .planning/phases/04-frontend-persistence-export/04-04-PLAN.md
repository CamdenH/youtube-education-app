---
phase: 04-frontend-persistence-export
plan: "04"
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - index.html
autonomous: false
requirements:
  - FRNT-05
  - FRNT-10

must_haves:
  truths:
    - "User has visually confirmed the full UI works end-to-end"
    - "Dark mode renders correctly"
    - "375px viewport is usable with no horizontal overflow"
    - "Module cards expand/collapse correctly"
  artifacts:
    - path: "index.html"
      provides: "Complete production frontend"
      contains: "renderCourse"
  key_links: []
---

<objective>
Visual and functional verification of the complete frontend. Run the full test suite, then verify the UI manually at 375px and desktop widths.

Purpose: Catch visual/interaction bugs that unit tests cannot cover before marking the phase complete.
Output: Verified UI; any fixes applied directly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-persistence-export/04-CONTEXT.md
@.planning/phases/04-frontend-persistence-export/04-UI-SPEC.md
@.planning/phases/04-frontend-persistence-export/04-03-SUMMARY.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix any failures</name>
  <files>index.html, tests/unit/frontend.test.js</files>
  <read_first>
    - index.html (current state after Plan 03)
    - tests/unit/frontend.test.js (all test cases)
  </read_first>
  <action>
Run the full test suite: `node --test --test-concurrency=1`. If any tests fail, fix the issue in the relevant file. Do not change test expectations — fix the production code to match.

Additionally, verify that all existing tests from prior phases still pass (sse.test.js, server.test.js, etc.).
  </action>
  <verify>
    <automated>cd /Users/camdenhosn/PycharmProjects/Youtube-Education-App && node --test --test-concurrency=1</automated>
  </verify>
  <acceptance_criteria>
    - `node --test --test-concurrency=1` exits 0
    - All frontend.test.js tests pass
    - All prior phase tests pass (sse.test.js, server.test.js, etc.)
  </acceptance_criteria>
  <done>Full test suite green — all unit tests from all phases pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of complete frontend</name>
  <files>index.html</files>
  <action>
Present the complete frontend for user verification. The user should run `node server.js` and test all interactive flows at both desktop and 375px viewport widths. Any issues identified will be fixed before marking approved.
  </action>
  <verify>User confirms all 12 verification steps pass</verify>
  <done>User types "approved" after confirming all flows work correctly.</done>
  <what-built>Complete frontend UI: search form, SSE pipeline animation, course rendering with modules/videos/questions/badges, localStorage history, watched checkboxes, recent searches pills, markdown export, dark mode, responsive layout.</what-built>
  <how-to-verify>
    1. Run `node server.js` and open http://localhost:3000
    2. Verify dark mode: page has dark background (#0f0f0f), light text
    3. Verify search form: subject input with placeholder "Enter a subject to learn...", skill level dropdown defaulting to "All Levels", "Generate course" button
    4. Verify empty state: heading "What do you want to learn?" with description text
    5. Submit a search (e.g., "javascript basics" / Beginner):
       - Button changes to "Generating..."
       - Pipeline steps appear with animated dots
       - Course renders with title, overview, prerequisites, watch time
       - First module expanded, rest collapsed
       - Video cards show thumbnails, titles, channel, duration, score badge
       - Questions accordion collapses by default
       - "Reveal thinking points" button visible inside questions
       - "Mark as watched" checkbox visible
    6. Check "Mark as watched" on a video, reload page — checkbox should still be checked when restoring from history
    7. Click "Export course" — markdown file downloads
    8. Recent searches: verify pills appear below search form
    9. History panel: verify course appears in "Recent courses" list
    10. Click a history item — verify course restores exactly
    11. Open DevTools responsive mode at 375px — verify no horizontal overflow, video cards stack vertically
    12. Verify clicking a recent search pill pre-fills the subject input
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan — no new code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No new threats — verification only |
</threat_model>

<verification>
1. `node --test --test-concurrency=1` — full suite green
2. Manual visual verification at 375px and desktop widths
3. Manual functional verification of all interactive flows
</verification>

<success_criteria>
- All unit tests pass across all phases
- User confirms UI works end-to-end
- Dark mode, responsive layout, and all interactions verified
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-persistence-export/04-04-SUMMARY.md`
</output>
